<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ระบบจัดเก็บชั่วโมงจิตอาสา | Student Activity Tracker</title>
    <!-- โหลด Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- โหลด Font Google (Prompt) -->
    <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- ตั้งค่า สีและ Font ของ Tailwind -->
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['Prompt', 'sans-serif'] },
                    colors: {
                        primary: '#FBBF24', // เหลือง
                        primaryDark: '#D97706', // เหลืองเข้ม (Hover)
                        secondary: '#4B5563', // เทา
                        bgLight: '#F3F4F6' // เทาอ่อน (Background)
                    }
                }
            }
        }
    </script>
    <style>
        /* ซ่อน Element ที่ไม่ได้ใช้งาน */
        .hidden-section { display: none !important; }
    </style>
</head>
<body class="bg-bgLight text-secondary min-h-screen font-sans">

    <!-- ==================== 1. หน้า Login ==================== -->
    <section id="loginView" class="flex items-center justify-center min-h-screen">
        <div class="bg-white p-8 rounded-xl shadow-lg w-full max-w-md border-t-4 border-primary">
            <div class="text-center mb-8">
                <h1 class="text-2xl font-bold text-gray-800">ระบบจัดเก็บกิจกรรม</h1>
                <p class="text-sm text-gray-500 mt-2">เข้าสู่ระบบเพื่อจัดการข้อมูลของคุณ</p>
            </div>
            
            <form id="loginForm" class="space-y-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700">อีเมล</label>
                    <input type="email" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-primary focus:border-primary" placeholder="email@example.com">
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700">รหัสผ่าน</label>
                    <input type="password" class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-primary focus:border-primary" placeholder="••••••••">
                </div>
                <button type="button" class="w-full bg-primary hover:bg-primaryDark text-white font-semibold py-2 px-4 rounded-lg transition duration-200 shadow-md">
                    เข้าสู่ระบบ
                </button>
            </form>

            <!-- ปุ่มจำลองสำหรับการทดสอบ UI (ลบออกเมื่อต่อ Firebase) -->
            <div class="mt-6 border-t pt-4 text-center">
                <p class="text-xs text-gray-400 mb-2">ปุ่มสำหรับทดสอบ UI (Demo)</p>
                <div class="flex space-x-2">
                    <button onclick="switchView('studentView')" class="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-700 py-2 rounded-lg text-sm">👩‍🎓 เข้าฐานะนักศึกษา</button>
                    <button onclick="switchView('adminView')" class="flex-1 bg-gray-800 hover:bg-gray-900 text-white py-2 rounded-lg text-sm">👨‍💻 เข้าฐานะแอดมิน</button>
                </div>
            </div>
        </div>
    </section>

    <!-- ==================== 2. หน้า Student Dashboard ==================== -->
    <section id="studentView" class="hidden-section max-w-5xl mx-auto p-6">
        <div class="flex justify-between items-center mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-800">แดชบอร์ดนักศึกษา</h1>
                <p class="text-gray-500">ยินดีต้อนรับ, รหัสนักศึกษา 64100000</p>
            </div>
            <button onclick="switchView('loginView')" class="text-sm bg-white border border-gray-300 hover:bg-gray-100 px-4 py-2 rounded-lg shadow-sm">ออกจากระบบ</button>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <!-- ฟอร์มเพิ่มข้อมูล -->
            <div class="lg:col-span-1 bg-white p-6 rounded-xl shadow-sm border border-gray-200">
                <h2 class="text-xl font-semibold mb-4 border-b pb-2 border-primary">📝 เพิ่มกิจกรรมใหม่</h2>
                <form id="activityForm" class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium mb-1">ชื่อกิจกรรม <span class="text-red-500">*</span></label>
                        <input type="text" id="actName" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-primary transition" placeholder="เช่น ค่ายอาสาพัฒนาชนบท">
                        <p class="text-red-500 text-xs mt-1 hidden" id="err-actName">กรุณากรอกชื่อกิจกรรม</p>
                    </div>
                    <div>
                        <label class="block text-sm font-medium mb-1">วันที่จัดกิจกรรม <span class="text-red-500">*</span></label>
                        <input type="date" id="actDate" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-primary transition">
                        <p class="text-red-500 text-xs mt-1 hidden" id="err-actDate">กรุณาเลือกวันที่</p>
                    </div>
                    <div>
                        <label class="block text-sm font-medium mb-1">ผู้รับรองกิจกรรม <span class="text-red-500">*</span></label>
                        <input type="text" id="actCertifier" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-primary transition" placeholder="ชื่ออาจารย์ หรือ หน่วยงาน">
                        <p class="text-red-500 text-xs mt-1 hidden" id="err-actCertifier">กรุณากรอกผู้รับรอง</p>
                    </div>
                    <div>
                        <label class="block text-sm font-medium mb-1">แนบไฟล์หลักฐาน (PDF/รูปภาพ) <span class="text-red-500">*</span></label>
                        <input type="file" id="actFile" class="w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-yellow-50 file:text-primaryDark hover:file:bg-yellow-100 border border-gray-300 rounded-lg p-1">
                        <p class="text-red-500 text-xs mt-1 hidden" id="err-actFile">กรุณาแนบไฟล์หลักฐาน</p>
                    </div>
                    <button type="button" onclick="submitActivity()" class="w-full bg-primary hover:bg-primaryDark text-white font-semibold py-2 px-4 rounded-lg transition shadow">
                        💾 บันทึกข้อมูล
                    </button>
                </form>
            </div>

            <!-- ตารางประวัติ -->
            <div class="lg:col-span-2 bg-white p-6 rounded-xl shadow-sm border border-gray-200">
                <h2 class="text-xl font-semibold mb-4 border-b pb-2 border-primary">ประวัติการเข้าร่วมกิจกรรม</h2>
                <div class="overflow-x-auto">
                    <table class="w-full text-left border-collapse">
                        <thead>
                            <tr class="bg-gray-100 text-gray-600 text-sm">
                                <th class="p-3 rounded-tl-lg">วันที่</th>
                                <th class="p-3">ชื่อกิจกรรม</th>
                                <th class="p-3">สถานะ</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr class="border-b hover:bg-gray-50">
                                <td class="p-3 text-sm">15 พ.ค. 2026</td>
                                <td class="p-3 text-sm">อบรม First Aid เบื้องต้น</td>
                                <td class="p-3"><span class="bg-green-100 text-green-700 px-2 py-1 rounded-full text-xs font-semibold">อนุมัติแล้ว</span></td>
                            </tr>
                            <tr class="border-b hover:bg-gray-50">
                                <td class="p-3 text-sm">20 พ.ค. 2026</td>
                                <td class="p-3 text-sm">ปลูกป่าชายเลน</td>
                                <td class="p-3"><span class="bg-yellow-100 text-yellow-700 px-2 py-1 rounded-full text-xs font-semibold">รอตรวจสอบ</span></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </section>

    <!-- ==================== 3. หน้า Admin Dashboard ==================== -->
    <section id="adminView" class="hidden-section max-w-6xl mx-auto p-6">
        <div class="flex justify-between items-center mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-800">ระบบจัดการผู้ดูแล (Admin)</h1>
                <p class="text-gray-500">ตรวจสอบและอนุมัติกิจกรรมของนักศึกษา</p>
            </div>
            <button onclick="switchView('loginView')" class="text-sm bg-white border border-gray-300 hover:bg-gray-100 px-4 py-2 rounded-lg shadow-sm">ออกจากระบบ</button>
        </div>

        <!-- ส่วนค้นหาข้อมูล -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-200 mb-6">
            <div class="flex flex-col md:flex-row gap-4 items-end">
                <div class="w-full md:w-1/3">
                    <label class="block text-sm font-medium mb-1">ค้นหาจาก</label>
                    <select class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-secondary">
                        <option>รหัสนักศึกษา</option>
                        <option>ชื่อ - นามสกุล</option>
                        <option>เลขประจำตัวประชาชน</option>
                    </select>
                </div>
                <div class="w-full md:w-1/2">
                    <label class="block text-sm font-medium mb-1">คำค้นหา</label>
                    <input type="text" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-secondary" placeholder="พิมพ์คำค้นหาที่นี่...">
                </div>
                <div class="w-full md:w-auto">
                    <button class="w-full bg-secondary hover:bg-gray-700 text-white font-semibold py-2 px-6 rounded-lg transition shadow">
                        🔍 ค้นหา
                    </button>
                </div>
            </div>
        </div>

        <!-- ตารางข้อมูลรวม -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="bg-gray-800 text-white text-sm">
                            <th class="p-4">รหัสนักศึกษา</th>
                            <th class="p-4">ชื่อ-สกุล</th>
                            <th class="p-4">กิจกรรมที่ส่ง</th>
                            <th class="p-4">หลักฐาน</th>
                            <th class="p-4 text-center">การจัดการ</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr class="border-b hover:bg-gray-50">
                            <td class="p-4 text-sm font-medium">64100001</td>
                            <td class="p-4 text-sm">สมชาย ใจดี</td>
                            <td class="p-4 text-sm">จิตอาสาทาสีโรงเรียน<br><span class="text-xs text-gray-400">10 พ.ค. 2026</span></td>
                            <td class="p-4"><a href="#" class="text-blue-500 hover:underline text-sm flex items-center">📄 ดูไฟล์</a></td>
                            <td class="p-4 flex justify-center gap-2">
                                <button class="bg-green-500 hover:bg-green-600 text-white text-xs px-3 py-1 rounded shadow">อนุมัติ</button>
                                <button class="bg-red-500 hover:bg-red-600 text-white text-xs px-3 py-1 rounded shadow">ปฏิเสธ</button>
                            </td>
                        </tr>
                        <!-- ตัวอย่างคนที่ 2 -->
                        <tr class="border-b hover:bg-gray-50">
                            <td class="p-4 text-sm font-medium">64100002</td>
                            <td class="p-4 text-sm">สมหญิง รักเรียน</td>
                            <td class="p-4 text-sm">ค่ายคอมพิวเตอร์เพื่อน้อง<br><span class="text-xs text-gray-400">12 พ.ค. 2026</span></td>
                            <td class="p-4"><a href="#" class="text-blue-500 hover:underline text-sm flex items-center">🖼️ ดูรูปภาพ</a></td>
                            <td class="p-4 flex justify-center gap-2">
                                <span class="text-green-600 text-sm font-bold">✔ อนุมัติแล้ว</span>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </section>

    <!-- ==================== JavaScript Logic ==================== -->
    <script>
        // ฟังก์ชันสลับหน้า UI (จำลองการ Login)
        function switchView(viewId) {
            document.getElementById('loginView').classList.add('hidden-section');
            document.getElementById('studentView').classList.add('hidden-section');
            document.getElementById('adminView').classList.add('hidden-section');
            
            document.getElementById(viewId).classList.remove('hidden-section');
            
            // ล้างค่า Error ทุกครั้งที่เปลี่ยนหน้า
            if(viewId === 'studentView') resetValidation();
        }

        // ฟังก์ชันตรวจสอบและบันทึกข้อมูล (ฝั่งนักศึกษา)
        function submitActivity() {
            let isValid = true;
            
            // ดึงค่าจาก Input
            const name = document.getElementById('actName');
            const date = document.getElementById('actDate');
            const certifier = document.getElementById('actCertifier');
            const file = document.getElementById('actFile');

            // ตรวจสอบ ชื่อกิจกรรม
            if (!name.value.trim()) {
                showError(name, 'err-actName');
                isValid = false;
            } else { clearError(name, 'err-actName'); }

            // ตรวจสอบ วันที่
            if (!date.value) {
                showError(date, 'err-actDate');
                isValid = false;
            } else { clearError(date, 'err-actDate'); }

            // ตรวจสอบ ผู้รับรอง
            if (!certifier.value.trim()) {
                showError(certifier, 'err-actCertifier');
                isValid = false;
            } else { clearError(certifier, 'err-actCertifier'); }

            // ตรวจสอบ ไฟล์
            if (file.files.length === 0) {
                showError(file, 'err-actFile');
                isValid = false;
            } else { clearError(file, 'err-actFile'); }

            // ถ้ายืนยันข้อมูลครบถ้วน
            if (isValid) {
                alert('ข้อมูลถูกต้อง! (เตรียมส่งข้อมูลและไฟล์ขึ้น Firebase)');
                /* 
                 * ตรงนี้คือจุดที่คุณจะเขียนโค้ดต่อกับ Firebase
                 * 1. อัปโหลด file.files[0] ไปยัง Firebase Storage เพื่อเอา URL
                 * 2. เอา URL และ Text (name, date, certifier) บันทึกลง Firestore
                 */
                
                // ล้างฟอร์มหลังจำลองการส่งเสร็จ
                document.getElementById('activityForm').reset();
            }
        }

        // Utility: แสดงขอบสีแดงและข้อความแจ้งเตือน
        function showError(inputElement, errorId) {
            inputElement.classList.add('border-red-500', 'bg-red-50');
            inputElement.classList.remove('border-gray-300');
            document.getElementById(errorId).classList.remove('hidden');
        }

        // Utility: ล้างขอบสีแดง
        function clearError(inputElement, errorId) {
            inputElement.classList.remove('border-red-500', 'bg-red-50');
            inputElement.classList.add('border-gray-300');
            document.getElementById(errorId).classList.add('hidden');
        }

        // Utility: ล้าง Error ทั้งหมด
        function resetValidation() {
            const inputs = ['actName', 'actDate', 'actCertifier', 'actFile'];
            inputs.forEach(id => {
                const el = document.getElementById(id);
                if(el) clearError(el, 'err-' + id);
            });
            document.getElementById('activityForm').reset();
        }
    </script>
</body>
</html>
